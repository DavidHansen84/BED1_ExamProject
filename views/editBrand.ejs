<!DOCTYPE html>
<html>
  <head>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/navbar.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h2 class="p-3"> Edit brand details</h2>
    
            <h3>Id   <%= brand.Id %></h3>

            <form>
                <label for="brandName">Name</label>
                <input type="text" id="brandName" name="brandName" value="<%= brand.Name %>">
            </form>
            
            
            <button class="btn-sm btn-warning" onclick="captureValues()">Edit</button>

    
    <script>
        async function captureValues() {
            var newName = document.getElementById("brandName").value;
    
            await editBrand(<%=brand.Id%>, newName);
        }
    </script>
    <script src="../../js/Brands.js"></script>
  </body>
</html>
